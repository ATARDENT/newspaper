# Tailscale API access token
# Tailscale admin console -> Settings -> Keys -> Generate API access token
TAILSCALE_API_KEY=

# Tailnet name. Use your tailnet's domain (e.g. example.com) or "-" for the
# default tailnet associated with an OAuth client.
TAILSCALE_TAILNET=
